# Ferramentas

Tribunal TRF2: Processo expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_trf2_processo_consultar`
**Input**: `numero_processo` (opcional), `oab` (opcional), `cpf` (opcional), `cnpj` (opcional), `nome_parte` (opcional)

Tribunal TRF2: Processo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TRF2: Processo
```
